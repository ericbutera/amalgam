# graphql-golang-client

This tool uses [khan graphql](https://github.com/Khan/genqlient/) to generate a typed Go client for a GraphQL API.

Run using `mise run generate-graph-golang-client` from the project root.
